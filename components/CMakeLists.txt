# =============================================================
# SDK components：基于底层驱动的功能库（协议 / 消息 / 升级 / 存储等）
# 通过 JL_CHIP_PLATFORM 选择平台子目录
# =============================================================
add_subdirectory(${JL_CHIP_PLATFORM})